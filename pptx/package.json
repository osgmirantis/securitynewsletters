{
  "name": "aikido-newsletter-deck",
  "version": "1.0.0",
  "private": true,
  "description": "Builds a Product Security Newsletter PowerPoint from report.json",
  "bin": { "build-deck": "build_deck.js" },
  "scripts": { "build": "node build_deck.js" },
  "dependencies": {
    "pptxgenjs": "^3.12.0"
  }
}
